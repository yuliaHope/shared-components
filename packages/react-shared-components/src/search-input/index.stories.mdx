import Filter from '.';
import { Meta, Story, Preview, Description, Props } from '@storybook/addon-docs/blocks';
import { useState } from '@storybook/addons';

import { IconBath } from '../icons';

<Meta
  title={'Filter'}
  component={Filter}  />

<h1>Filter</h1>

<Preview>
  <Story name='WithInputHandler'>{() => {
    const options = [{value: 'option1', label: 'Option 1'}, {value: 'option2', label: 'Option 2'}, {value: 'option3', label: 'Option 3'}];
    const [select, setSelected] = useState('');
    const [searchOptions, setOptions] = useState([]);
    return (
        <div className="m-1">
            <div className="w-25">
                <Filter type="search" options={searchOptions} onInput={(value) => setOptions(value ? options : [])} placeholder='Search' />
            </div>
            <div className="w-25">
                <Filter type="select" options={options} onSelect={setSelected} placeholder='Search' />
            </div>
            <div>Selected value: {select.value} Selected label: {select.label}</div>
        </div>
    );
  }}</Story>
</Preview>


### Props

<Props of={Filter} />

#### Event handlers

- `onSelect` is called when the value selected

#### Children

Search Result component may be provided as children
